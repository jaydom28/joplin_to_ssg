# joplin_to_ssg
Uses the [Joplin Webclipper API](https://joplinapp.org/help/api/references/rest_api/) to export to a static site generator. Currently it only supports hugo because that is what I use, but there may be support added for other static site generators.
